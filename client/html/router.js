/*
Router.configure({
    layoutTemplate: 'layout'
});


Router.route('/addProduct', {
   name: "addProduct",
  // template:"addProduct"
});

Router.route('/showProducts', {
   name: "showProducts",
  // template:"addProduct"
});

Router.route('/', {
   name: "boot",
  // template:"addProduct"
});

Router.route('/productDetail', {
   name: "productDetail",
  // template:"addProduct"
});

Router.route('/myunderlyings', {
   name: "myunderlyings",
  // template:"addProduct"
});

Router.route('/mycalendar', {
   name: "mycalendar",
  // template:"addProduct"
});

Router.route('/productDetail/:isin', function () {
  var specificProduct = Products.findOne({isin: this.params.isin});
  this.render('productDetail', {data: specificProduct});

  Session.set('specificProduct',specificProduct)
});

*/
