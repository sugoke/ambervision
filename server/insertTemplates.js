import { Meteor } from 'meteor/meteor';

// Templates are now hardcoded in /imports/api/templates.js
// This file is no longer needed but kept for reference

Meteor.startup(async () => {
  console.log('ℹ️  Templates are now hardcoded - no database seeding needed');
});